# Desarrollo: Render por defecto (emails forgot/verify con Resend, misma API que App y prod).
# Para Goi Server solo en LAN sin emails: crea `.env.local` con VITE_API_URL=/api
VITE_API_URL=https://goi-server.onrender.com/api
