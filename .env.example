# Goi Web — desarrollo (copia a `.env.development` o `.env.local` y reinicia Vite)
#
# Recomendado: proxy en vite.config.ts (solo abres http://localhost:5173)
VITE_API_URL=/api
#
# Alternativa sin proxy (llamadas directas a Goi Server):
# VITE_API_URL=http://localhost:4000/api
#
# Producción (Vercel): VITE_API_URL se define en vercel.json → Render.
# API prod: https://goi-server.onrender.com/api
