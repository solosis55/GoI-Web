/** Sesión JWT + usuario (localStorage). */
export const AUTH_STORAGE_KEY = "goi-auth";

/** Tras `auth:expired`: `stale` (AUTH_SESSION_STALE) o `expired` (sessionStorage, una lectura). */
export const SESSION_EXPIRED_STORAGE_KEY = "goi:sessionExpired";

/** Tema claro/oscuro (localStorage). */
export const THEME_STORAGE_KEY = "goi:theme";
